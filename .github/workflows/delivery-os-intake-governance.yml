# Enable Delivery OS – Intake Governance (self-test)

name: Enable Delivery OS – Intake Governance

on:
  issues:
    types: [opened, edited, labeled]
  pull_request:
    types: [opened, edited, labeled]

jobs:
  call-intake-governance:
    uses: jkaweesi22/github-delivery-operating-system/.github/workflows/intake-governance.yml@main
    with:
      intake_label: "intake"
    secrets: inherit
